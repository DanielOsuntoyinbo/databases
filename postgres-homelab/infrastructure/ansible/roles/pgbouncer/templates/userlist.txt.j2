# userlist.txt — managed by Ansible
# Contains only pgbouncer_auth — used by PgBouncer to connect to PostgreSQL
# and run auth_query to look up other users dynamically.
# All other users are authenticated via auth_query against pg_shadow.
"pgbouncer_auth" "{{ pgbouncer_auth_password }}"
